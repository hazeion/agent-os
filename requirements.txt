# Mentat runtime dependencies
# Keep this file intentionally small and pinned for reproducible local setup.
# Standard-library modules are omitted.

# Google Calendar read-only integration
# google.auth.transport.requests is imported directly in server.py, so requests
# must stay explicit here instead of relying on unrelated environment packages.
google-api-python-client==2.194.0
google-auth==2.49.2
idna==3.18
requests==2.33.0
websockets==15.0.1

# Owner-private WebAuthn and recovery-code authority.
fido2==2.2.1
argon2-cffi==25.1.0

# Structural validation for generated raster artifacts and Console images.
Pillow==12.3.0

# Cross-platform IANA timezone data for zoneinfo (required on Windows).
tzdata==2026.3
