# Direct native build inputs. Compile on each supported platform with
# pip-tools 7.6.0 and hashes before changing a public release toolchain.
-r requirements.txt
build==1.3.0
pyinstaller==6.21.0
wheel==0.47.0
# Include both platform-specific PyInstaller helpers in the shared lock. They
# are pure Python and harmless on the other build host; this keeps one exact,
# hash-checked input valid for both trusted native jobs.
macholib==1.16.4
pefile==2024.8.26
pywin32-ctypes==0.2.3
setuptools==80.9.0
