# Reproducible local native-bundle tooling. Release signing happens only in a
# protected trusted environment and never from this file.
-r requirements.txt
build==1.3.0
pyinstaller==6.21.0
wheel==0.47.0
