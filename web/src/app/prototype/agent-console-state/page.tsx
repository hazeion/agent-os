// THROWAWAY PROTOTYPE #132: this route lives only on the prototype branch.

import { AgentConsoleStatePrototype } from "./prototype-client";

export const metadata = {
  title: "Agent Console state prototype · Mentat",
};

export default function AgentConsoleStatePrototypePage() {
  return <AgentConsoleStatePrototype />;
}
