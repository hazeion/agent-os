[build-system]
requires = ["setuptools==83.0.0", "wheel==0.47.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mentat-local"
dynamic = ["version"]
description = "A local-first multi-agent operations console and orchestrator."
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Hazeion" }]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Web Environment",
  "Operating System :: MacOS",
  "Operating System :: Microsoft :: Windows",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]
dependencies = [
  "google-api-python-client==2.194.0",
  "google-auth==2.49.2",
  "Pillow==12.3.0",
  "requests==2.33.0",
  "tzdata==2026.3",
]

[project.scripts]
mentat = "mentat.cli:main"

[tool.setuptools.dynamic]
version = { attr = "mentat.version.__version__" }

[tool.setuptools]
packages = ["mentat"]
include-package-data = false
py-modules = [
  "agent_registry",
  "agent_registry_migration",
  "agent_runtime",
  "agent_console_artifacts",
  "agent_console_attachments",
  "agent_console_telemetry",
  "agent_run_history",
  "command_manifest",
  "codex_runtime",
  "data_backup_restore",
  "data_layout",
  "data_migration",
  "data_schema",
  "delegation_artifacts",
  "diagnostics_bundle",
  "health_checks",
  "hermes_browser_events",
  "hermes_event_refresh",
  "hermes_kanban",
  "hermes_profile_creation",
  "hermes_profile_deletion",
  "hermes_profile_identity",
  "hermes_profiles",
  "hermes_runtime",
  "hermes_provider_switching",
  "hermes_skills",
  "hermes_stock_compatibility",
  "hermes_transport",
  "hermes_webhooks",
  "hermes_webhook_health",
  "hermes_webhook_store",
  "json_store",
  "mentat_db",
  "mentat_lifecycle",
  "orchestration_service",
  "private_console_migration",
  "private_console_unit",
  "private_state",
  "remote_hermes",
  "run_repository",
  "runtime_config",
  "server",
  "task_planning",
  "task_repository",
]
