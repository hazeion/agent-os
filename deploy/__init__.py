"""Checked-in, disabled deployment profiles.

Deployment profiles are deliberately separate from local runtime code.  Importing
one must never install software, bind a listener, or change a launcher.
"""
